import {StyleSheet} from "react-native"

export const globalStyles = StyleSheet.create({
    titleText: {
        fontFamily: "poppins",
        fontSize: 20, 
        fontWeight: 'bold'
    },
    normalText: {
        fontFamily: "poppins",
        fontSize: 20, 
        fontWeight: 'normal'
    }
});

export const theme = {
    primary:"lightgreen",
    secondary: "#fff",
}